[project]
name = "sampling"
version = "0.1.0"
description = "Interactive digital audio sampling and Nyquist-Shannon demonstration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anywidget",
    "marimo",
    "numpy",
    "scipy",
    "soundfile",
    "traitlets",
]
